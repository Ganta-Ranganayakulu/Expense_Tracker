# Expense_Tracker
Expense Tracker to track your expenses in daily life 
Developed using the ReactJS

# Features
Calculate the expenses 
Add the salary 
Subtract the Expenses 
Displays the Balance amount after expenses 


# Project Setup
To install the nodemodules use the command `npm install` and to run the application use the command `npm run dev`
